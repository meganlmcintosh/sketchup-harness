#!/usr/bin/env bash
# MCP driver, scoped to this repo. Register with:
#   claude mcp add supex -- "$(pwd)/bin/mcp"
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export SUPEX_PROJECT_ROOT="$ROOT"
exec "$ROOT/vendor/supex/mcp" "$@"
