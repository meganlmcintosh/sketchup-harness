# frozen_string_literal: true

source 'https://rubygems.org'

gem 'yard', '~> 0.9'

# SketchUp-specific YARD plugin
# Note: This may already be included in the sketchup-api-stubs submodule
# but we include it here for explicit dependency management
gem 'yard-sketchup', '~> 1.6'

gem 'rubocop', '~> 1.75', require: false
