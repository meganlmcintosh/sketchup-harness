# macOS
.DS_Store

# SketchUp
*.skb
~*.skp
AutoSave_*.skp

# Python
__pycache__/
*.py[cod]
.venv/
.uv/
.pytest_cache/
.ruff_cache/

# Written by uv when the vendored driver is run directly, not through bin/
# (whose lockfile is supex-env/uv.lock); scripts/update-vendor.sh deletes it.
vendor/supex/driver/uv.lock

# Harness scratch: SketchUp logs, screenshots
.tmp/

# Generated from each project's plan.yaml; rebuild with ./bin/plan build
projects/*/out/

# Ruby
.bundle/
vendor/bundle/

# Local tooling
.mise.local.toml
.claude/settings.local.json
